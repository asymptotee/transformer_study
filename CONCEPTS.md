# 概念深入：QKV、KV cache、记忆 vs 泛化

本文档收录学习过程中深挖的几个核心概念，是对 `ARCHITECTURE.md`（讲代码怎么运作）
的补充——这里讲的是**背后的原理**。

目录：

1. [QKV 深入](#一qkv-深入)
2. [KV cache 与"算 token 的 K/V"](#二kv-cache-与算-token-的-kv)
3. [反转 vs 续写：算法与记忆的对照](#三反转-vs-续写算法与记忆的对照)
4. [模型怎么"决定"学规则还是记忆](#四模型怎么决定学规则还是记忆)
5. ["装得下"：从容量到矩阵自由度](#五装得下从容量到矩阵自由度)
6. [数据规模引发的质变：背不动了会怎样](#六数据规模引发的质变背不动了会怎样)
7. [分词：字符级 vs BPE](#七分词字符级-vs-bpe)
8. [涌现：量变如何变成质变](#八涌现量变如何变成质变)
9. [预训练与后训练：LLM 的炼成](#九预训练与后训练llm-的炼成)
10. [微调概念辨析：SFT、LoRA、后训练的关系](#十微调概念辨析sftlora后训练的关系)
11. [量化：给模型减肥](#十一量化给模型减肥)
12. [上下文：模型每次预测时"看到"的历史](#十二上下文模型每次预测时看到的历史)

---

## 一、QKV 深入

Q、K、V = **Query / Key / Value**，是注意力里的三个角色。

### 1. 直觉：图书馆找书

| 角色 | 类比 | 作用 |
|---|---|---|
| **Q**uery | 你脑子里的问题 | "我想找什么" |
| **K**ey | 每本书的标题/标签 | "我这本书是关于什么的" |
| **V**alue | 书的实际内容 | "真正要被取走的信息" |

过程：拿 Q 和每本书的 K 算相似度（点积）→ softmax 归一成权重 → 按权重对各本书的
V 加权求和 → 得到汇总答案。写成公式（`model.py:38`）：

```
Attention(Q,K,V) = softmax(Q·Kᵀ / √d_k) · V
                   └──── Q 和 K 算权重 ────┘   └ 对 V 求和 ┘
```

### 2. 工程里的对应

```python
# model.py:88-91 —— 三个投影矩阵，把输入分别变成 Q、K、V
self.w_q = nn.Linear(d_model, d_model, bias=False)   # W_Q
self.w_k = nn.Linear(d_model, d_model, bias=False)   # W_K
self.w_v = nn.Linear(d_model, d_model, bias=False)   # W_V
self.w_o = nn.Linear(d_model, d_model, bias=False)   # 输出混合 W_O

# model.py:98-100 —— 投影 + 拆头，得到真正的 q/k/v 张量
q = self.w_q(q).view(...).transpose(1, 2)   # (B, H, T, d_k)
k = self.w_k(k).view(...).transpose(1, 2)
v = self.w_v(v).view(...).transpose(1, 2)
```

**关键：Q、K、V 不是三种不同的东西，而是同一段输入过三个不同的可学习矩阵
（W_Q、W_K、W_V）得到的三种"视角"。** 这三个矩阵就是训练时被调整的参数。

### 3. 三种注意力，区别只在 q/k/v 喂的是谁

代码里就是同一个 `MultiHeadAttention` 类：

| 注意力 | q 来自 | k、v 来自 | 在哪 |
|---|---|---|---|
| encoder 自注意力 | 源序列 | 源序列（q=k=v） | `model.py:175` |
| decoder 因果自注意力 | 目标序列 | 目标序列（q=k=v） | `model.py:203` |
| **交叉注意力** | decoder | **encoder 输出** | `model.py:208` |

最后一行就是反转任务里画出反对角线的那个——decoder 的 Q 去查 encoder 的 K/V。

---

## 二、KV cache 与"算 token 的 K/V"

### 1. 先搞懂："算 token 的 K/V" 是什么

**说白了就是做两次矩阵乘法。** 一个 token 经 embedding + 位置编码变成向量 `x`
（d_model 维），然后：

```
x = [0.5, -0.2, 1.1, 0.7]
        │ × W_K（矩阵乘法，model.py:99）
        ▼
K_c = [0.9, 0.3, -0.5, 1.2]     ← c 的 Key
        │ x 再 × W_V（model.py:100）
        ▼
V_c = [-0.1, 0.8, 0.4, 0.6]     ← c 的 Value
```

K 和 V 都由同一个 `x` 加工而来，但 W_K、W_V 是两个不同的矩阵，所以得到两个不同
向量。分工：

| | K（Key） | V（Value） |
|---|---|---|
| 角色 | 标签/把手 | 实际内容 |
| 用在哪 | 和 Q 算点积，决定"分多少注意力给 c" | 被注意力权重加权，决定"从 c 取走什么" |
| 一句话 | **别人怎么找到我** | **找到我后拿到什么** |

### 2. KV cache 解决什么问题

自回归生成时，`generate()`（`model.py:306`）每一步都把**整个已生成序列**重新喂进
decoder：

```python
for _ in range(max_new_tokens):
    logits = self.decode(ys, enc_out, src_mask)[:, -1]   # ys 每步都在变长
```

大量重复计算：

```
步骤 0:  ys=[bos]      → 算 [bos] 的 K/V
步骤 1:  ys=[bos,c]    → 算 [bos,c] 的 K/V      ← bos 的 K/V 又算了一遍（浪费）
步骤 2:  ys=[bos,c,b]  → 算 [bos,c,b] 的 K/V    ← bos、c 又算了（浪费）
```

生成 n 个 token，总量 `1+2+...+n = O(n²)`，越长越慢。

### 3. 核心洞察：旧 token 的 K/V 不会变

因为**因果掩码**：token c 在固定位置时只能看到自己和前面的 token，看不到后面还没
生成的。所以：

```
生成第 5 个 token 时，c 的向量 x 不变（它看不到第 5 个）
  → x 不变 → K_c = x·W_K 不变，V_c = x·W_V 也不变
```

既然算出来就永不再变，何必每步重算？**算一次、存起来**——这就是 KV cache。
而 Q 不行：每生成一个新 token 都要为它算全新的 Q 去查询。

```
带 cache：
步骤 0:  算 [bos] 的 K/V → 存入 cache
步骤 1:  只算 [c] 的 K/V → 追加进 cache；注意力用 cache 里的 [bos,c]
步骤 2:  只算 [b] 的 K/V → 追加；注意力用 [bos,c,b]
```

每步只做 1 个 token 的投影（最贵的 d_model×d_model 矩阵乘），FFN 也只跑新 token。

### 4. 为什么 cache 要按层存

decoder 有 N 层（`model.py:235`），**每层都有自己的 W_K、W_V**，同一 token 在每层
的 K/V 都不同，都得各存一份：

```
cache 形状: (n_layers, 2, B, H, T, d_k)
             每层一份  ↑K和V两份  ↑头数 ↑序列长（随生成不断变长）
```

这就是为什么 LLM 圈总在谈 KV cache：长上下文推理时它既是速度瓶颈也是显存瓶颈。
GQA（分组查询注意力）、MQA 本质都是"减少 K/V 的头数"来给 cache 瘦身。

### 5. 这个工程有 KV cache 吗？

- **decoder 自注意力的 KV cache：没有。** `generate()` 每步全量重算，朴素写法。
  对学习是故意的——逻辑直白，序列最长才 64，O(n²) 无所谓。
- **但有半个表亲**：`model.py:315` 在循环外只编码一次 encoder——

  ```python
  enc_out, src_mask = self.encode(src)   # 只编码一次，循环里反复用
  ```

  交叉注意力里 encoder 那侧的 K/V 永远不变，所以缓存起来（这里直接缓存了
  `enc_out`）。可以说"encoder 侧的 cache 做了，decoder 侧的没做"。

| | QKV | KV cache |
|---|---|---|
| 工程里有吗 | ✅ 核心（`w_q/w_k/w_v`） | ❌ decoder 侧没有 |
| 相关代码 | `model.py:88-107` | 朴素重算：`model.py:318-319` |
| "半个"对应 | — | encoder 只编码一次：`model.py:315` |

---

## 三、反转 vs 续写：算法与记忆的对照

两个任务是整个工程最有教学价值的一组对照——**同一套代码，只改数据切法，就逼出
"学算法"和"背答案"两种截然不同的学习行为。**

### 1. 联系：几乎是同一个程序

最惊人的事实：**`model.py` 里没有一行 `if task`。** 模型完全不知道训的是哪个任务。
两者共享模型架构、数据管道、训练循环、推理；**唯一的代码分叉**在 `data.py:63`：

```python
if task == "reverse":
    pairs.append((line, line[::-1]))        # 整行 → 反转
elif task == "complete":
    mid = len(line) // 2
    pairs.append((line[:mid], line[mid:]))  # 前半 → 后半
```

区别**不在代码，而在数据的结构**——数据结构决定了模型能学到什么。

### 2. 区别：一张表看清

| 维度 | reverse（反转） | complete（续写） |
|---|---|---|
| 数据对 | `(床前明月光, 光月明前床)` | `(床前, 明月光)` |
| src/tgt 关系 | 有**确定规则**（第 i 位 ↔ 第 L-1-i 位） | **无通用规则**，纯靠"这是哪首诗" |
| 本质 | 学算法（alignment） | 记忆语料（association） |
| 训练数据 | 真实语料 **+ 4000 条随机串** | 纯真实语料 |
| 能否泛化 | ✅ 没见过的串也会反转 | ❌ 没见过的句子答不对 |
| 挑 checkpoint | `val_loss`（看验证集） | `train_loss`（看训练集） |
| 交叉注意力 | 清晰的**反对角线** | 无明显结构（联想记忆） |

### 3. 为什么本质不同

**reverse 是"算法任务"**：反转有对所有输入都成立的规则。模型学会"输出第 i 位看
输入第 L-1-i 位"，就能处理任何字符串。`demo_matrices.py` 里那条反对角线就是这条
规则的物理形态。但它差点学不会（README 坑 2）：只有 300 行时模型背下来更省力，
于是 `train_loss` 降到 0.25、`val_exact` 却是 0。混入 4000 条随机串后，空间大到
背不完，模型被迫学算法。

**complete 是"记忆任务"**："床前"后面接什么没有通用规则，唯一答案是"因为这是
李白那首诗"。模型只能把语料背下来建立联想，**注定不能泛化**。给它加随机串也没
意义——随机串根本没有"正确的后半句"可学。

### 4. 最微妙的一点：checkpoint 按什么挑（README 坑 3）

```python
# train.py:152 —— 挑选标准取决于任务性质
score = val_loss if args.select_by == "val_loss" else train_loss
```

- **reverse 按 `val_loss`**：会泛化，验证 loss 越低 = 真的越好。
- **complete 必须按 `train_loss`**：靠记忆，验证集永远是陌生句子。模型越没记住
  （训练越早期），输出越平滑通用，验证 loss 反而越低——按 val_loss 会精准挑出
  **最差**的模型！

一句话：**会泛化的任务看验证，靠记忆的任务看训练。**

### 5. 各自验证了什么

| | 回答的问题 | 类比 |
|---|---|---|
| reverse | "注意力机制能不能学会结构化对齐？" | 验证架构**正确性**的基准测试 |
| complete | "它能不能像语言模型一样记忆并续写？" | 接近真实 LLM（下一个 token 预测） |

reverse 是"考题"（有标准答案、可判分）；complete 是"预演"（真实 LLM 本质就是在
海量语料上做 complete，只不过语料大到能涌现泛化，而 300 行只够记忆）。

> **同样的网络，喂"有规则的数据"它就学规则（泛化），喂"无规则的数据"它就背答案
> （记忆）。任务性质不由代码决定，而由数据里有没有可学的结构决定。**

---

## 四、模型怎么"决定"学规则还是记忆

### 1. 模型不"决定"——它根本没有"自己"

"模型决定学规则还是记忆"是方便的比喻。严格说：**模型内部没有任何选择、没有意图。
** 它只是一堆参数，训练就是梯度下降不断微调这些数字让 loss 变小。这个过程是
**盲目的、局部的**——每步只朝"当前能让 loss 下降最快"的方向挪一小步，既不长远
规划，也不欣赏优雅的解法。

所以"学规则 vs 记忆"不是模型做的选择题，而是**它最终滑进了哪个坑**。

### 2. 关键：记忆和规则都能让训练 loss 降到 0

在 300 行语料上：

- **记忆方案**：把每行原样存进权重，输入第 k 行就吐第 k 行的反转。train_loss → 0 ✓
- **规则方案**：权重实现"第 i 位看第 L-1-i 位"。train_loss → 0 ✓

两个方案在**训练集上表现一模一样**。梯度下降只看训练 loss，那它凭什么选？
答案：**它选"更容易到达"的那个。**

### 3. "容易到达"：宽靶子 vs 窄靶子

想象所有参数组合构成一片地形，loss 是高度，梯度下降就是往低处走：

```
300 行语料时的地形：
   记忆解 ← 又宽又浅的大盆地（很容易走进去）
   规则解 ← 又窄又深的针眼（要精确对齐才能命中）
```

- **记忆是"宽靶子"**：75 万参数有海量方式把 300 个输入输出对硬凑出来，不需要
  精巧结构，随便走走就拟合了。
- **规则是"窄靶子"**：要让权重精确协作、实现与内容无关的位置算法，是非常特定的
  参数组合，盲目小步走很难恰好命中。

于是模型"选了"记忆——不是因为它好，而是因为它**近、好够到**。这就是 README 坑 2
的真相：它真把训练集背下来了，但验证集的陌生句子从没见过，背不出来。

### 4. 加 4000 条随机串 = 改变地形

```
加数据后的地形：
   记忆解 ← 盆地消失（4000 条随机串彼此毫无规律，硬背又难又慢）
   规则解 ← 唯一能同时拟合所有样本的低谷
```

关键洞察：**这 4000 条随机串之间，唯一的共同规律就是"反转"本身。** 它们内容随机、
互不相关，没有任何逐条特征可供取巧。要让 loss 在所有样本上都低，除了实现反转算法
别无他法。

梯度下降还是那个盲目过程，还在往低处走——但现在通向低 loss 的路**只剩规则这一条**。
这就是"被迫"的准确含义：

> **不是模型想通了要学规则，而是我们把"记忆"这个偷懒选项从可达解里删掉了。
> 它依然在盲目降 loss，只是现在不学规则就降不下去。**

### 5. 更深一层：压缩即学习

模型参数有限（75 万），数据可近乎无限。**用有限参数拟合无限数据，唯一的办法是
压缩——找到规律，用规律代替逐条存储。**

- 300 行：参数 >> 数据，装得下，不用压缩，直接背。
- 海量随机串：装不下，必须压缩，而反转规律就是最强的压缩。找到它 = 学会它。

这其实就是"学习"的定义：从个别样本中提炼出能覆盖未见过样本的规律。

### 6. 为什么验证集是唯一的裁判

| | 记忆方案 | 规则方案 |
|---|---|---|
| 训练集 loss | 0 | 0 |
| **验证集（陌生数据）** | **全错** | **全对** |

训练 loss 被两种方案同时骗过，**只有没见过的数据才能区分"背"和"会"。**

### 7. 这是普遍现象

"模型倾向走最省力的捷径"是深度学习的通病。很多技巧本质都是**"让记忆变难、逼模型
学结构"**：

- **数据增强**（本项目用的）：数据多到背不完
- **Dropout**：随机关掉神经元，没法依赖特定记忆路径
- **正则化 / 早停**：惩罚复杂拟合，压低死记硬背的吸引力

它们都不"教"模型该学什么，只是**改造地形**，让正确的解成为最容易到达的解。

> **模型从不"决定"学什么。它只是盲目往 loss 低处走。数据少时记忆是最近的坑，
> 它掉进去；数据多到背不完时记忆的坑被填平，通向低 loss 的路只剩"学规则"一条——
> 它别无选择，只能学会。所谓"被迫"，就是我们通过数据设计把偷懒选项从地形上抹掉了。**

---

## 五、"装得下"：从容量到矩阵自由度

### 1. 容量视角：参数 = 一本页数固定的笔记本

把全部参数想象成一本笔记本，每个参数是一小格可写数字的格子：

- 笔记本**页数是死的**（参数数量固定），训练只是往格子里填不同数字，格子总数不变。
- 模型记住的东西全写在这本笔记本里，没有别处。

```
数据量 < 笔记本容量  →  能逐字抄进去  →  "装得下" → 能死记硬背
数据量 > 笔记本容量  →  抄不完        →  "装不下" → 只能写规律
```

**这个工程的具体数字**：

| | 信息量 |
|---|---|
| 模型参数 | 约 75 万个 × 32 位浮点 ≈ **3 MB** 的笔记本 |
| 训练数据 | 300 行短诗 ≈ **几 KB** |

3 MB 对几 KB——笔记本比数据大千倍。把 300 行连答案原样抄进去，零头都用不完。
所以模型完全有空间把每行单独记下来，不需要提炼规律。

**规律的本质是压缩：用 O(1) 的参数覆盖 O(任意多) 的数据。** "第 i 位输出 = 第
L-1-i 位输入"这句话，不管输入 300 条还是 3 亿条都同样适用，占用的参数却是固定的
一小撮。当数据多到抄不完，唯一能塞进有限笔记本的办法就是写下规律而非抄写内容。

### 2. 矩阵视角：不是"原样存"，而是"自由度 vs 约束"

先破除误解：**数据并没有原样存进矩阵——权重矩阵不是数据库，没有"第 k 行放第 k 条
数据"这回事。**

```python
self.w_q = nn.Linear(d_model, d_model)   # 一个 (128,128) 矩阵
```

这个 W_Q 对**每一个输入**都做同样的变换，里面没有任何一行对应某条训练数据。
**所有矩阵都是"共享的加工机器"，不是"分格子的储物柜"。** 唯一像查表的是
embedding，但它**每个字符一行**，不是每条数据一行。

**那记忆在哪？—— 溶解在权重里（分布式）。** 每条数据的信息被打散编码进许多权重；
每个权重也同时参与表示许多条数据。类比**全息照片**：每小片都含整张图的信息，
而不是"左眼存在左上角"。

**准确的矩阵图像——自由度 vs 约束：**

1. 形状 `(m, n)` 的矩阵有 `m×n` 个可独立调节的数字 = **自由度**。
2. 一条训练数据 `(x_k, y_k)` 是一个**约束**："输入 x_k 时，所有矩阵连乘后输出
   必须等于 y_k"。把复合函数看成 `f`，训练要求 `f(x_1)=y_1, ..., f(x_N)=y_N`。
3. **"装得下" = 旋钮数 >> 约束数**：

   ```
   旋钮（参数）75 万  >>  约束（数据）几千个
   ```

   约束远少于旋钮 → 严重欠定的方程组 → 满足所有约束的权重取值有无穷多组 →
   模型随便扭扭就能拟合全部数据。这就是"装得下"。

**最小矩阵例子**（矩阵的列 = 它对基向量的输出）：

```
W = [ 3  7 ]      列1=[3,5]，列2=[7,9]
    [ 5  9 ]

W·[1,0] = [3,5]    ← 存下关联 e1 → [3,5]
W·[0,1] = [7,9]    ← 存下关联 e2 → [7,9]
```

4 个数字恰好够指定 2 个输出向量（4 个约束）。现在它"满了"，塞不进第 3 个独立
关联——微观版的装得下/装不下：**自由度数 = 能任意指定的约束数。** 放大到整网：
75 万旋钮 → 能伺候约 75 万个约束 → 300 条短句远远装得下 → 逐条记忆。

### 3. 记忆 vs 规则，在矩阵里长什么样

两者都是同一批矩阵里的数字，区别在于编码的是"一份清单"还是"一套程序"。

**记忆态**：权重被调成只对那几条特定输入产生正确输出，输入别的东西时输出未定义、
随机（那些旋钮从没被新数据约束过）。

```
  输出
   │      ╱╲    ╱  ← 为穿过每个训练点，曲线扭得很丑
   │   ╱╲╱  ╲╱╱     （死记硬背的形状）
   │  ●  ●  ●  ●   ← 训练点
   └──────────── 输入
   训练点上正确，训练点外乱来 → 不泛化
```

**规则态**：权重 settles 成与内容无关的结构。反转任务里就是交叉注意力的反对角线：

```
        a     b     c     d     e
  预测e [ 0     0     0     0    0.99 ]   ← 不管内容是什么，
  预测d [ 0     0     0    0.78   0  ]      第 i 位永远看第 L-1-i 位
  预测c [ 0     0    0.83   0     0  ]      一套"程序"，
  ...                                        对没见过的输入同样成立
```

### 4. 数据一多为什么只能走规则

```
数据少（300 条）：
  约束 << 旋钮 → 欠定 → 无穷多组权重能拟合
  → 梯度下降随便找到一组"逐条伺候"的（记忆，宽靶子）→ 背

数据多到装不下（海量随机串）：
  约束逼近/超过旋钮 → 没有余量逐条硬凑
  → 唯一能同时满足所有约束的是"有结构"的权重（规则）
  → 因为规则用固定旋钮覆盖无限约束 = 压缩
```

> **数据不是原样存进矩阵的——矩阵是共享的加工机器，记忆是分布式"溶解"在全体权重
> 里的。"装得下"的矩阵含义是：可调参数（自由度）远多于训练数据施加的约束，于是
> 存在海量权重取值能把每条数据逐一拟合，模型便滑进"逐条硬凑"的记忆解；一旦数据
> 多到约束超过自由度，硬凑不再可行，矩阵才被迫组织成一套与内容无关的结构——那就是
> 规则。**

---

## 六、数据规模引发的质变：背不动了会怎样

承接第三、四、五节，回答一个自然的问题：

> **如果续写（complete）的训练数据大于参数量、导致不能靠记忆了，会是什么结果？**

答案其实就是真实 LLM 的诞生过程。把这个工程的 complete 任务数据无限放大，
得到的不是"一个背不下来的失败模型"，而是**一个开始真正"理解"语言的模型**。

### 1. 直接回答：它从"背"转向"学规律"

回忆第五节的结论：数据多到"装不下"时，有限参数唯一的选择是**压缩**，而压缩 =
找规律。对 complete 任务来说，这个规律不再是反转那种干净的位置算法，而是**语言的
统计结构**：

```
数据 << 参数（现在的 300 行）：
  装得下 → 逐行背诵 → "床前"→"明月光" 是因为"背过这首"

数据 >> 参数（放大到全唐诗 + 海量文本）：
  装不下 → 被迫压缩 → 学到：
    · 格律：五言后面大概率接什么平仄
    · 语义："月"常和"明""光""霜"共现
    · 语法：动词后面接名词
    · 意境套路：写景之后常接抒情
```

模型不再"记得"某首诗，而是**学会了"唐诗大概长什么样"**。于是它能续写**从没
见过的句子**，甚至创作出像模像样的新诗。

### 2. 这是个连续谱，不是非此即彼

```
数据量 / 参数量
─────────────────────────────────────────────▶
  远小于          相当            远大于
    │               │                │
  纯记忆         记忆+规律        纯压缩/泛化
（现在 complete）              （真实 LLM）
```

- 这个工程的 complete：在最左，纯记忆，验证集全错。
- 真实 LLM（GPT/Claude）：在最右——训练文本远超参数能记的量，**所以它们根本不靠
  记忆，而是靠压缩出来的语言结构**。你问它一个从没见过的问题它能答，原因就在这。

**这个工程用 300 行演示的"记忆"，和万亿 token 训练出的 LLM 的"理解"，是同一枚
硬币的两端。** 区别只是数据规模把模型从谱的左端推到了右端。

### 3. 能观察到的具体变化

如果真把 complete 的语料从 300 行扩到几十万行、模型相应放大：

| 现象 | 现在（小数据） | 数据远超参数后 |
|---|---|---|
| 验证集 loss | 一直很高（陌生句子答不对） | **开始下降**（能泛化了） |
| 挑 checkpoint | 必须用 `train_loss`（坑 3） | **可以改回 `val_loss`** |
| 陌生 prompt | 全错 | 大概率续对 |
| 注意力模式 | 一团无结构（联想记忆） | **语义关联**（"月"关注"明/光"） |
| 训练/验证 loss | 差距巨大 | 逐渐靠拢 |

注意第二行——**README 坑 3 那个"必须按 train_loss 挑"的补丁，在大数据下会失效，
可以改回 val_loss。** 因为任务性质变了。

### 4. 和反转的关键不同：规律是"概率的"而非"确定的"

| | reverse | 放大后的 complete |
|---|---|---|
| 规律类型 | **唯一确定**（第 i 位 = 第 L-1-i 位） | **概率分布**（多个续写都合理） |
| 学完后 loss | → 0（完全可预测） | **停在某个非零值**（语言本身的熵） |
| 生成方式 | 贪心即可，输出唯一 | 需要**温度采样**，因为答案不唯一 |

"床前明月光"之后，"疑是地上霜"是最可能的，但"举头望明月"在别的语境也成立——
语言天生不确定。所以再好的语言模型 loss 也降不到 0，只能逼近**语言的熵**
（不可消除的不确定性）。这正是 `generate.py` 要有 `--temperature` 的根本原因：
**因为语言本身就不是确定的，模型学的是分布而非映射。**

### 5. 最深的洞察：任务性质会随规模改变

把前面所有概念串起来，得到这个工程最值钱的教训：

> **"这个任务是靠记忆还是靠泛化"不是任务的固有属性，而是数据规模的函数。**

- 同一套 complete 代码，300 行时是记忆任务（看 train_loss）；
- 数据涨到远超参数时，**它自己变成了泛化任务**（看 val_loss）。

这不是玩具实验的巧合——**这正是 LLM 革命的核心机制**：当 next-token 预测
（= complete）的数据规模大到装不下，模型为了压缩，被迫学出了语法、语义、推理、
世界知识。"预测下一个词"这个看似简单的目标，在足够大的数据下，**涌现**出了
看起来像"理解"的能力。

换句话说：**你不需要教模型"理解"，你只需要给它多到背不完的文本，让它为了压缩
而自己去发现结构。** 这和反转任务"加 4000 条随机串逼它学算法"是完全同一个原理，
只是规模差了十个数量级。

### 6. 一句话总结

> **如果 complete 的数据大到超过参数容量，模型就背不动了，只能去压缩数据——
> 而压缩自然语言，就是学习它的统计结构。于是它从"背下每一首诗"变成"学会唐诗
> 怎么写"，开始能续写没见过的句子，验证集 loss 下降，checkpoint 可以改按
> val_loss 挑。这个从"记忆"到"泛化"的转变，不是量变的累积，而是任务性质的
> 质变——把同一个 next-token 目标放大万亿倍，就是你今天用的每一个大语言模型。**

> 这一节的理论预测在 `stage2_scaling/` 里做了可控实验验证（同一模型、同一任务，
> 只变数据量）：大数据把验证集 loss 压低约 650 倍、续写准确率做到满分。

---

## 七、分词：字符级 vs BPE

阶段四用到 BPE 分词。这一节讲清楚"分词"是什么、字符级和 BPE 的区别——
它是理解阶段四的前提。

### 1. 根本问题：文本怎么变成数字

神经网络只能对数字运算。所以第一步必须把文本切成一个个**单位（token）**，
每个单位编一个 id，再查成向量喂给模型：

```
"我们回到故乡"  →  切成 token  →  每个 token 一个 id  →  查成向量  →  喂给模型
```

核心问题就是：**用什么当"单位"？** "字符级"和"BPE"就是两种不同的切法。

### 2. 字符级：一字一 token（阶段一~三用的）

最简单的切法——一个字就是一个 token（`CharTokenizer` 干的）：

```
"我们回到故乡"  →  [我, 们, 回, 到, 故, 乡]  →  6 个 token
```

把语料里所有字收集起来，每字一个 id。鲁迅语料 4592 种字 → 词表 4592。

- **优点**：极简、不需要分词工具、永无生词（任何文本都能用字拼出）。
- **缺点**（阶段四要解决的）：
  - **序列太长**：每字占一个位置，64 的窗口只能看 64 个字。
  - **语义被打散**：`逻辑` 是两个无关 token `逻`、`辑`，模型要从零学"它俩总在一起"，
    词的边界全丢了。

### 3. BPE：从字符自动合并出词（阶段四用的）

**BPE = Byte-Pair Encoding（字节对编码）**，真实 LLM（GPT 系列）的分词方式。
它是字符级和"词级"之间的中间路线：

> 从字符出发，反复合并语料里**出现最频繁的相邻对**，自动学出一套"子词"。

算法（`stage4_scaling_bpe/bpe.py` 就是这么做的）：

```
1. 把语料全拆成字符
2. 统计所有"相邻字符对"的次数，合并最高的那一对，变成一个"新 token"
3. 把新 token 当整体，重新统计、再合并最高频的对
4. 重复 N 次（阶段四用了 600 次）
```

例：`我`+`们` 挨着出现最多 → 合并成 `我们`，从此是一个 token；下一轮 `故`+`乡`
最高 → 合并成 `故乡`…… 常见词自动变成单 token，生僻字（如 `邂`）从没高频共现，
就保持成一个字。阶段四学出的合并就是真实词语：**母亲、立刻、如此、回到、故乡**。

同一句子的两种切法：

```
"我们回到故乡"
字符级:  [我, 们, 回, 到, 故, 乡]    6 个 token，每个是字
BPE:    [我们, 回到, 故乡]          3 个 token，每个是词（短一半，且有语义）
```

### 4. 三种切法对比

| | 字符级 | 词级 | **BPE（子词）** |
|---|---|---|---|
| 单位 | 单个字 | 完整的词 | 常见词 + 生僻字 |
| 序列长度 | 最长 | 最短 | 中等 |
| 词表大小 | 小（几千字） | 巨大（所有词） | 中等 |
| 生词问题 | 无 | **严重**（新词没 id） | **无**（退回字符即可） |
| 需要分词工具 | 不需要 | 需要（中文尤其难） | **不需要，从数据学** |

BPE 的精髓：**常见组合当词（短、有语义），生僻内容退回字（永不 OOV）**——
两头的好处都占，所以成了 LLM 的标配。

### 5. 回到这个项目

| | 阶段一~三 | 阶段四 |
|---|---|---|
| 分词 | `CharTokenizer`（字符级） | `BPETokenizer`（BPE） |
| "逻辑"怎么表示 | `逻`、`辑` 两个 token | 若高频则 `逻辑` 一个 token |
| 鲁迅词表 | 4592（全是字） | 5192（字 + 600 个合并词） |
| 64 窗口看到 | 64 个字 | 约 77 个字（序列更短） |

阶段四那个"诚实的结果"由此好理解了：BPE 词表更大（5192，预测更难）、小规模没
训透，所以 loss 数字略逊；但它**在词块层面操作**，生成句法更顺。它的真正价值
（更短序列、词级语义）要在**大规模**才完全兑现——这正是 LLM 用它的原因，
也和第六节"规模引发质变"是同一个主题。

### 6. 一句话总结

> **字符级"一字一 token"，简单但把词拆碎、序列又长；BPE 从字符出发自动把高频
> 相邻字合并成词，做到"常见的是词、生僻的是字"，兼顾短序列、有语义、无生词——
> 这就是真实大模型的分词方式。**

---

## 八、涌现：量变如何变成质变

第六节说"规模引发质变"，连贯性、理解这些能力是"涌现"出来的。这一节讲清楚
"涌现"到底是什么——它是理解大模型最关键、也最容易被神化的概念。

### 1. 定义：量变到质变

> **涌现 = 某种能力在小规模时完全不存在，规模大到某个临界点后突然出现。**

它不是"逐渐变好"，而是**质的跳跃**：低于阈值，模型做这个任务跟随机猜差不多；
高过阈值，它突然就会了。整体表现出了部分所没有的性质。

### 2. 关键签名：loss 平滑下降，能力却跳跃

这是涌现最反直觉、也最核心的特征：

```
loss（预测误差）          能力（比如"多位数加法做对"）
   │\                         │
   │ \                        │          ┌──── 突然就会了
   │  \  平滑地一路下降        │          │
   │   \_____                 │__________┘
   └──────── 规模             └──────── 规模
        （量变，连续）              （质变，跳跃）
```

**训练损失随规模平滑下降（scaling law），但很多具体能力是"啪"地一下出现的。**

### 3. 为什么会这样

两个原因叠加：

1. **一个任务需要多个子技能同时达标。** 比如"解应用题"需要读懂题意 + 列式 + 计算 +
   单位换算。每个子技能都随规模**逐渐**变好，但任务只有在**全部**子技能都越过各自
   阈值时才算"会"——于是整体表现为突然会了。
2. **评判方式是"全对才算对"。** 用 exact-match 衡量时，做对 90% 步骤但最后一步错
   = 0 分。底层能力在平滑进步，但"要么全对要么零分"的指标把平滑进步显示成了跳跃。

### 4. 典型例子

| 能力 | 小模型 | 大模型（过阈值后） |
|---|---|---|
| 多位数加法 | 完全不会 | 突然会 |
| 看几个例子就学会（few-shot） | 例子没用 | 看例子就会 |
| 一步步推理（chain-of-thought） | 不会 | 涌现 |
| **写出连贯文本** | **词语沙拉** | **连贯文章** |

最后一行就是这个项目：小模型写"我冒了严寒风的，我就是一个人"，足够大的模型写
连贯文章。**连贯性不是被教会的，是规模到了之后涌现的。**

### 5. 回到这个项目

阶段四的缩放实验看到了**涌现的一半**：

```
small  val 5.29  → 生成破碎
medium val 4.84  → 稍好
large  val 4.53  → 出现对话结构（"你不肯说？"赵儿子说）
```

- **loss 平滑下降**（5.29→4.84→4.53）——亲眼看到了 scaling law（量变）。
- **生成质量也在变好**——质变的苗头。但规模范围太小（36万~207万），还没到真正
  涌现的临界点，所以是渐变而非突变。

真正的涌现要把规模再放大几个数量级——某一刻，量变积累的内部表示突然丰富到足以
支撑"连贯""推理"，它们就**作为"预测下一个 token"的副产品**冒出来。这正是第六节
的结论：**你不需要教模型"理解"，规模到了，理解自己涌现。** 阶段二的"记忆→泛化"
转变也是涌现的一种——数据过临界点，任务性质从"背"突然变成"会"。

### 6. 一个诚实的补充（学界争议）

有研究（如 2023《Are Emergent Abilities a Mirage?》）指出：涌现的"突然"**部分是
指标的假象**。如果不用 exact-match，改用平滑指标（逐 token 准确率、log 概率），
那条"突然跳跃"的曲线其实变成了平滑上升。更准确的说法：

> **底层能力随规模平滑增长（真实、连续）；但用"全对才算对"的眼光去看时，它呈现为
> 突然涌现。涌现既是真实的（能力确实在大规模才可用），也部分是测量方式造成的观感。**

这不影响核心结论——能力确实需要规模才能兑现——只是提醒别把"跳跃"想得太神秘。

### 7. 涌现与 Transformer 的关系

一个常见误解：以为涌现是 Transformer 的专属魔法。其实——

> **涌现不是 Transformer 特有的，它是规模现象；Transformer 是第一个能被放大到
> 涌现区间的架构。** 二者是"现象"和"载体"的关系，不是"原因"和"结果"。

**涌现本身与架构无关**：它的本质是"参数 + 数据 + 算力大到一定程度，能力突然出现"，
回答的是"**多大**才会质变"，而非"**什么结构**才会质变"。

**Transformer 的角色是"载体"**——不是随便什么架构都能被放大到涌现的规模：

| 特性 | 为什么对"抵达涌现"重要 |
|---|---|
| 可并行训练 | 注意力一次算完所有位置（不像 RNN 串行），训得动几十亿参数 |
| 训得深、稳定 | 残差 + LayerNorm 让深层网络训得动 |
| 干净的 scaling law | loss 随规模呈幂律下降，可预测地"变大变强" |
| 注意力擅长组合/长程依赖 | 让规模带来的压缩转化成语义、语法、推理 |

在它之前，RNN/LSTM 因串行计算、长程依赖难训，堆不到那个规模，也就看不到涌现。
《Attention Is All You Need》的真正意义，是**打开了通往规模化（从而通往涌现）的大门**。

**这个项目就是最直接的证据**：你手写的 Transformer 和 GPT 是**同一个架构**，
唯一区别是规模——你的几十万~两百万参数产出词语沙拉，GPT 几十亿参数涌现出连贯
与推理。你在阶段四看到的 loss 平滑下降，是同一条 scaling law 的起点，延伸十几个
数量级就是涌现。**二者之间没有架构鸿沟，只有规模鸿沟。**

注意：**没有任何单一组件是涌现的开关。** 涌现来自合力——可扩展架构（Transformer，
载体）× 巨大规模（真正驱动力）× next-token 目标 × 海量数据。Transformer 提供"能被
无限放大"的载体，真正把能力压出来的，是规模。

### 8. 类比与一句话总结

**水加热**：温度从 0 平滑升到 100℃（量变），但"沸腾"在 100℃ 突然出现（相变）。
单个水分子从不"沸腾"，沸腾是大量分子集体行为的涌现。LLM 一样：单个参数不"理解"，
但几十亿参数协同、规模跨过临界点后，"理解"作为集体行为涌现。

> **涌现 = 量变（loss 随规模平滑下降）积累到临界点，引发质变（某项能力突然出现）。
> 它源于"任务需要多个子技能同时达标"和"全对才算对的评判方式"。连贯性、推理不是
> 被教会的，而是规模到了之后，作为"预测下一个 token"的副产品自己冒出来的——这正是
> 大模型之所以"大"的根本原因。但记得：底层进步是平滑的，"突然"一部分是测量造成的观感。**

---

## 九、预训练与后训练：LLM 的炼成

前面讲了注意力和规模，这一节讲一个 LLM 实际是**怎么被训练出来的**——分两大阶段：
预训练（学知识和语言）和后训练（调教成好用的助手）。

### 1. 全景：两阶段流水线

```
海量原始文本（万亿 token）
      │
      ▼
┌──────────────┐
│ ① 预训练       │  next-token 预测，从零学语言+知识  → 基座模型（只会续写）
│  Pretraining  │  最贵：千张 GPU × 数月
└──────────────┘
      │
      ▼  ━━━ ② 后训练（把基座调教成好用的助手）━━━
┌──────────────┐
│  SFT 监督微调  │  在(指令,回答)对上微调  → 学会"应答格式"
└──────────────┘
      │
      ▼
┌──────────────┐
│  RLHF / DPO   │  按人类偏好对齐  → 更有用、更安全
└──────────────┘
      ▼
   能聊天的助手
```

### 2. 预训练：一个目标做到极致

预训练的全部可浓缩成一个目标——**预测下一个 token**：

```
给定前文 x₁…xₜ，预测 xₜ₊₁，最大化 Σ log P(xₜ₊₁ | x₁…xₜ)
```

就是阶段三~五做的 next-token 训练，一字不差；区别只在规模（你用 62 万字符 +
353 万参数，真实预训练用万亿 token + 几千亿参数）。

这个目标看似简单，但**为了把下一个词猜准，模型被迫在权重里建起对世界的隐式模型**：
要预测"水的沸点是__"得懂物理，要预测代码下一行得懂逻辑。这正是第五节"压缩即学习"
的极致——预测即压缩，压缩即建模。

- **数据**：网页、书籍、维基、论文、**代码**（带来推理能力），经去重、过滤、调配比。
  **知识主要在这一步注入权重**（参数化记忆）。
- **规模**：遵循幂律（scaling law），且参数与数据要配套增长（Chinchilla：约每参数
  配 20 token）。
- **代价**：一次前沿预训练动辄数千万到上亿美元——大模型烧钱的根源。

### 3. 基座模型：强大但"不会做人"

预训练产出**基座模型**：博学，但只会"续写"、不会"应答"。

```
你输入:  "中国的首都是哪里？"
基座模型: "中国的首都是哪里？日本的首都是哪里？法国的首都是哪里？……"
         ↑ 它把问题当成"问卷的开头"接着编，而不是回答
```

因为它只是按统计规律续写，**没有"我在帮用户解答"这个概念**。这就是为什么需要后训练。

### 4. 后训练：从"续写"到"应答"

后训练把"文本的建模者"变成"听指令、有帮助、够安全的助手"。它比预训练**便宜得多**
（数据少、几个 epoch），但对"好不好用"**影响极大**——你感受到的"AI 好聪明贴心"，
大部分来自后训练。通常两步：**SFT → 对齐**。

### 5. SFT（监督微调）：教它"怎么应答"

**数据**：几万~几百万条 `(指令, 高质量回答)` 对，带角色模板：

```
<user> 中国的首都是哪里？ </user>
<assistant> 中国的首都是北京。 </assistant>
```

**训练**：还是 next-token，但有个**关键技巧——loss 掩码**：

```
只对 assistant 的回答算 loss，不对 user 的问题算：
  [user 问题]  [assistant 回答]
   ✗ 不算        ✓ 算 loss
```

因为目标是教模型**生成好回答**，而不是**复述问题**。这就是"对哪些位置算
cross-entropy"的选择。

**效果与局限**：SFT 教会应答的**格式、风格、"有帮助"的样子**，但**主要教行为、
不注入知识**——答出的内容仍是预训练学到的，SFT 只是教它"如何调用"。它也只能
**模仿**示范回答，难以表达细腻偏好或"该拒绝什么"，这需要下一步。

### 6. 对齐：RLHF 与 DPO

**动机**：有些要求难写成范例，但人**一眼能判好坏**（"这个更安全""那个更啰嗦"）。

**RLHF（三步）**：① 对同一问题让模型生成多个回答、人排序 → ② 训一个"奖励模型"
预测人类打分 → ③ 用强化学习（PPO）让模型朝高分进化（加 KL 惩罚防跑偏）。
更有用、更安全、会拒绝，但复杂、贵、不稳。

**DPO（直接偏好优化）**：推导出闭式解，**跳过奖励模型**，直接用"好回答 vs 坏回答"
的偏好对优化（升好降坏，相对参考模型）。效果接近 RLHF，但更简单、稳、便宜，
现在很流行。

### 7. 后训练的本质：激发而非灌输

后训练（尤其 SFT）**主要是激发预训练已有的能力，而非灌输新能力**。证据：很多能力
（如多步推理）在基座里"藏着"，SFT 或恰当提示就能引出——说明知识早在预训练学到，
后训练只教**何时、如何**用。这与第八节"涌现"呼应：能力在预训练的规模中涌现，
后训练把它们对齐到人类需要的方向。

### 8. 对照与连回项目

| | 预训练 | 后训练 |
|---|---|---|
| 目标 | 预测下一个 token（建模文本） | 让模型有用、安全、听指令 |
| 数据 | 万亿 token 原始文本 | 几万~几百万 (指令,回答) + 偏好对 |
| 注入什么 | **知识 + 语言能力** | **行为 + 偏好**（激发已有知识） |
| 产物 | 基座模型（只会续写） | 助手模型（会对话） |
| 代价 | 极贵（数月、数千万美元） | 便宜得多（数小时~数天） |
| 你的项目 | 阶段三~五 | 计划中的 D（SFT） |

连回项目：你的阶段三~五就是**微缩预训练**，训出的鲁迅模型正是"基座模型"——
给它"我冒了严寒"它续写，而不是回答关于鲁迅的问题，恰是基座的典型行为。计划做的
D 就是**微缩 SFT**：把它微调成"能回答鲁迅问题"，你会亲手用上 loss 掩码。SFT 后
它会从"接着编"变成"回答问题"，但**答不出鲁迅语料之外的东西**——因为那些知识从没
在预训练注入。这正是"后训练激发、预训练注入"最直观的体现。

> **预训练用"预测下一个 token"在万亿文本上学到知识和语言（贵、注入知识）；
> 后训练（SFT + 对齐）把它调教成听指令、有用、安全的助手（便宜、激发行为）。
> 知识在预训练注入，行为在后训练激发——你做的阶段三~五是前者，D 是后者。**

---

## 十、微调概念辨析：SFT、LoRA、后训练的关系

SFT、LoRA、微调、后训练这些词常被混为一谈。这一节把它们彻底理清——核心是认清
**它们不在同一个维度上**。

### 1. 核心：它们在不同的维度

把相关概念按**两个正交的轴**分开，混淆就消失了：

```
轴①「训练什么 / 为什么训」（目标、阶段）：
    预训练 → SFT → 对齐(RLHF/DPO) → 领域微调 ……

轴②「怎么训 / 怎么更新权重」（方法、技术）：
    全量微调 · LoRA · QLoRA · Adapter · Prefix-tuning ……
```

- **SFT 在轴①**：定义"用什么数据、什么目标"——在 `(指令,回答)` 对上监督训练，
  教模型应答。
- **LoRA 在轴②**：定义"用什么方式更新权重"——冻住原模型、只训低秩小矩阵。

一个是**目的**，一个是**手段**。所以"LoRA vs SFT"这个问法本身不成立——就像问
"炒菜 vs 铁锅"：炒菜是你要做的事，铁锅是工具。

### 2. LoRA 是什么（机制）

**解决的问题**：全量微调大模型太贵（700 亿模型为 10 个任务微调要存 10 份完整模型，
显存还要存所有参数的梯度和优化器状态）。

**核心洞察**：微调时权重的变化量 ΔW **内在维度很低**——只是"轻推"模型，不是重写
知识，所以用低秩近似就够。

**做法**：不直接学完整的 ΔW（d×k），而是分解成两个瘦矩阵 `ΔW = B·A`
（B 为 d×r，A 为 r×k，r 远小于 d、k，常取 4/8/16）：

```
            ┌──── W (d×k, 冻结) ────┐
   x  ─────┤                        ⊕ ──> h     h = W·x + B·A·x
            └─ A (r×k) ─ B (d×r) ───┘            （只训 A、B）
```

参数量对比（W 为 4096×4096，r=8）：全量 ΔW = 1677 万；LoRA 的 A+B = 6.6 万
（**仅 0.39%**）。两个细节：**B 初始化为 0**（从原始预训练行为起步）；用 **α/r**
缩放适配幅度。通常加在注意力的 W_Q/W_V 上。

**推理时**：可把 `W' = W + (α/r)·B·A` **合并**成一个矩阵 → 零额外延迟；也可不合并，
**热插拔**不同适配器（一个基座挂多个任务）。变体 **QLoRA**（基座量化到 4-bit + LoRA）
让 650 亿模型能在单卡微调。

### 3. 怎么组合

任何"训练目标"都可搭配任何"训练方法"：

| 目标 ＼ 方法 | 全量微调 | **LoRA** |
|---|---|---|
| **SFT** | 全量 SFT | **LoRA SFT**（如今最常见） |
| 领域微调 | 全量领域适配 | LoRA 领域适配 |
| 对齐（DPO） | 全量 DPO | LoRA DPO |

**"LoRA SFT"就是现在微调大模型的标准做法**：目标是 SFT（教应答），手段是 LoRA
（省显存地更新）。二者天经地义地组合。

### 4. LoRA 属于后训练吗

- **后训练是一个"阶段"**（预训练之后：SFT + 对齐）。
- **LoRA 是一种"技术"**，常被用在后训练里（尤其做 SFT 时），但它本身不是后训练的
  一个环节。
- LoRA 也能用在不算后训练的地方（如用 LoRA 做继续预训练/领域适配）。

所以：**LoRA 是后训练的常用工具，但不归属于后训练这个概念本身，它是横切的。**

### 5. 区别与联系

**区别**：

| | SFT | LoRA |
|---|---|---|
| 是什么 | 一种**训练目标/阶段** | 一种**参数更新方法** |
| 回答 | "训什么、为什么训" | "怎么训、怎么省着训" |
| 定义了 | 数据格式 + loss | 权重如何被低秩更新 |
| 对立面 | 预训练 / 无监督 | 全量微调 |

**联系**：实践中组合成"LoRA SFT"；**SFT 是目的，LoRA 是手段**；没有 LoRA 也能 SFT
（全量），没有 SFT 也能用 LoRA（做别的微调）。

### 6. 术语层级

很多混乱来自"微调"被滥用。严格层级：

```
微调 (fine-tuning)  ← 总称：在训好的模型上用新数据继续训
  ├─ 按目标分：SFT / 领域微调 / 对齐 ……   ← 轴①
  └─ 按方法分：全量 / LoRA / QLoRA ……     ← 轴②

后训练 (post-training) ← 阶段：预训练之后 = SFT + 对齐
```

"SFT 是微调的一种"（按目标）；"LoRA 是微调的一种实现"（按方法）；"后训练包含 SFT
和对齐"（按阶段）——三句话，三个分类角度。

### 7. 连回项目与一句话总结

计划做的 **D = SFT**（目标，轴①）。实现方式（轴②）有二：**全量 SFT**（你的模型才
353 万参数，毫无压力，**该用这个**）；**LoRA SFT**（对小模型没必要，但作为学 LoRA
的练习很合适）。最自然的安排：先全量 SFT 做出"续写→应答"（理解 SFT + loss 掩码），
再用 LoRA 重做一遍（理解低秩适配），对比参数量与效果——一次拿下轴①和轴②。

> **SFT 和 LoRA 不在一个维度：SFT 是"训练目标"（用监督数据教模型应答，属于后训练），
> LoRA 是"训练方法"（冻住原模型、只训低秩矩阵 ΔW=B·A，用 0.4% 参数达到接近全量的
> 效果，横切于各种微调）。它们不是二选一，而是目的与手段——实践中组合成"LoRA SFT"，
> 即用 LoRA 的方式做 SFT，这正是如今微调大模型的标准姿势。**

---

## 十一、量化：给模型减肥

LLM 越训越大，但**部署**的约束从来不是"装不装得下"这么简单。量化（quantization）
是让模型用更低精度的数字表示权重（和中间值），以极小质量代价换 4~8 倍的内存与
速度收益——它是大模型"跑得动、跑得快"的核心工程手段，也是 DGX Spark 这类
桌面级 AI 机器（主打 FP4 算力）的设计原点。

### 1. 先搞懂：为什么"瘦身"能直接变快

生成是**内存带宽瓶颈**，不是算力瓶颈。回顾一下生成的过程：每生成一个 token，
都要把全部权重从内存读一遍、算一遍。于是：

```
模型权重总量 = 每次生成要读的数据量
  fp16 的 70B 模型：每 token 读 140GB → 800GB/s 的卡上 ≈ 0.18s/token
  int4 的 70B 模型：每 token 读 35GB  → ≈ 0.04s/token（快 4 倍+）
```

权重瘦几倍，读取就快几倍——**和算得快不快无关**，纯粹是少搬数据。
这就是"量化提升速度"的全部秘密：不是算得快了，是搬得少了。

### 2. 核心机制：缩放映射（一个例子走完）

量化不是简单截断小数，而是把一段浮点范围**线性映射**到整数范围。
以 fp32 → int8（[-127, 127]）为例：

```
原始权重分布：[-2.0, 2.0]

1. 定 scale（缩放因子）= 范围 ÷ 整数台阶数 = 4.0 / 254 ≈ 0.0157
   含义：整数每差 1，代表的真实值差 0.0157

2. 量化（存的时候）：q = round(真实值 / scale) + zero_point
   例：0.5 → round(0.5 / 0.0157) = round(31.8) = 32

3. 反量化（算的时候）：真实值 ≈ (q - zero_point) × scale
   例：32 × 0.0157 ≈ 0.503     ← 误差 0.003
```

存储和搬运用 1 字节的整数，**计算前反量化回浮点**（或者用支持低精度乘法的硬件
直接算）。误差来自"取整"——[-2, 2] 之间只有 254 个台阶，真实值落不进整数台阶
就被舍入。量化 = 用可接受的舍入误差，换 4 倍瘦身。

### 3. 粒度：一把尺 vs 每行一把尺

scale 按多大的范围算，决定了误差怎么分布：

```
per-tensor：整层权重共用一把尺    ← 实现最简单，误差最大
per-channel：每个输出通道一把尺   ← 常用；各通道分布差异大时显著降误差
per-group：每 128 个权重一组      ← 4-bit 量化的标配，误差更小
```

直觉：不同输出通道的权重分布可能差两个数量级（有的在 ±0.1，有的在 ±10）。
共用一个 scale，小分布的直接被"碾平"——分粒度就是给不同分布的权重各配一把
合适的尺。

### 4. 量化什么：权重 / 激活 / KV cache

| 对象 | 是什么 | 为什么量化它 |
|---|---|---|
| **权重** | 模型参数，静态不变 | 内存 4 倍↓、读取 4 倍快（最常用、最简单） |
| **激活** | 前向计算的中间值，动态变化 | 计算直接走低精度（加速），但要校准数据定 scale |
| **KV cache** | 生成时的缓存（第二节讲过） | 长上下文时 KV cache 占显存大头，量化它 = 更长上下文 |

权重是"死"的（训完不变，scale 一次算好）；激活和 KV cache 是"活"的（每次
输入不同），量化它们需要小心得多。

### 5. 最大的坑：离群值

神经网络的权重里偶尔有几个**特别大**的——比中位数大几十倍。量化一把尺子量
全家时，为了装下离群值，scale 被拉得很大，**其余 99.9% 的权重全被压扁**在
相邻的整数台阶上，精度崩盘。

经典解法（LLM.int8() 论文）：先检测出离群列，**它们用 fp16 单独算**，
其余走 int8——"绝大部分走低精度，少数刺头走全精度"。这也是为什么 naive 的
"直接转 int8"经常翻车：不是量化本身不行，是没处理离群值。

### 6. 两类做法：PTQ 与 QAT

**PTQ（训练后量化）**——模型训完直接转，不碰训练：
- **RTN（round-to-nearest）**：直接舍入，最朴素；误差随层数累积
- **GPTQ**：量化某一行时，用还没量化的权重**补偿**它造成的误差——像高斯消元
  一样逐步修正，把"舍入误差"转嫁给还没量化完的行
- **AWQ**：不量化"重要"权重——按激活幅度判断哪些通道重要，保护它们
- 工业界主流是 PTQ：不需要重训，几小时搞定百亿级模型

**QAT（量化感知训练）**——训练时就模拟量化误差：前向用"假量化"（量化再
反量化），反向正常算梯度。模型在训练中**学着适应低精度**，效果最好，但要
重训，成本高。

### 7. 规模效应：为什么模型越大、量化越无损

反直觉但被反复验证的事实：**7B 模型量化到 int8 几乎不掉点，1B 以下小模型
掉点明显**。原因要从"冗余"理解：

- 大模型参数量远超任务所需的信息量——单个权重错一点，别的权重能补上
  （分布式冗余）；每个权重的"个体价值"低，砍精度无所谓
- 小模型的冗余少，每个权重都"珍贵"，量化误差直接体现在输出上

这又是一个"技术的意义在规模中兑现"的例子（和 BPE、涌现同族）：量化天然属于
大模型时代——**不是小模型不需要瘦身，是小模型瘦不起**。

### 8. 精度档位一览

| 档位 | 字节/权重 | 典型场景 |
|---|---|---|
| fp32 | 4 | 训练主权重（梯度更新要精度） |
| fp16 / bf16 | 2 | 训练计算、高质量推理 |
| int8 | 1 | 主流推理档，几乎无损 |
| int4 / fp4 | 0.5 | 消费级部署（70B 压到 ~35GB 才能进单卡） |

再往下还有 2-bit 的极端尝试（如 1.58-bit 论文），质量掉得厉害，属研究前沿。

### 9. 一句话总结

> **量化 = 用缩放映射把权重从 fp32 换成 int8/fp4 存储与计算：权重是"死"的所以
> 能粗存，模型是冗余的所以禁得起粗——以极小质量代价换 4~8 倍内存与生成速度
> （生成是带宽瓶颈，少搬数据就是快）。难点全在离群值处理与 scale 的粒度；
> 而"大模型量化几乎无损、小模型掉点明显"的规模效应，又一次印证了那条贯穿
> 全文的主线：很多技术的意义，要在规模中才兑现。**

---

## 十二、上下文：模型每次预测时"看到"的历史

### 1. 先定位：LLM 每时每刻在做什么

LLM 的本质操作只有一个：**看到一串 token，预测下一个**。

```
输入：你<eos>吃饭<eos>了吗<eos>今天天气真
                                        ↓
预测：下一个 token 最可能是"好"
```

模型做每次预测时能看到的**那串 token**，就是它的**上下文（context）**；
上下文窗口（context window）= 这串 token 允许的最长长度。

所以"128K 窗口的模型"不是"记忆力有 128K"，而是：**它做任何一次预测时，
最多能回头参考 128K 个 token**。

### 2. 两种记忆：参数里的知识 vs 上下文里的信息

这是理解 LLM 一切行为最关键的区分——模型有两种完全不同的"记忆"：

```
参数里的知识（长期记忆）：训练时从几十亿 token 里学来，训完固定、人人共享
  → "北京是首都"、语法、推理能力——沉淀在权重里，靠【训练】注入

上下文里的信息（短期记忆）：每次输入临时给的，用完即忘、随请求变化
  → 你刚说的话、上传的文档、系统提示词——靠【输入】提供
```

由此派生出一串重要推论：

- 模型和你**没有**"实时沟通"——它每次预测只是"参考上下文 + 参数知识"算概率
- **RAG** = 不训模型，把知识塞进上下文让它现读（参数知识不足时的补救手段）
- **in-context learning（上下文学习）** = 在上下文里放几个示例让模型照做，
  一个参数都不动——"学习"可以完全发生在上下文里
- **上下文劫持 / prompt 注入** = 往上下文里塞恶意指令——因为上下文对模型
  来说和系统指令一样"可信"，无法分辨来源

### 3. 为什么窗口不能无限长

三堵物理墙：

**① 注意力 O(n²)**：每个 token 要和上下文所有 token 两两算注意力。长度翻倍 →
计算量 4 倍、显存 4 倍。平方增长的墙，谁都绕不过。

**② 位置信息没见过**：模型训练时只见 ≤ 训练长度（block_size）的序列。给它
更长的输入，位置编码落在"没学过的地方"，能力会崩——这就是**外推**问题。
RoPE、ALiBi 等相对位置编码能缓解（相对位置比绝对位置更易泛化），但并非无限。

**③ KV cache 线性吃显存**：推理时每个已见 token 的 K/V 都要缓存（见第二节）。
上下文 128K × 每层每头 × 几十层——KV cache 轻松几十 GB。KV cache **量化**
（第十一节的技术）因此成为热点：缓存瘦身 = 直接换更长的窗口。

### 4. 扩大窗口的工程手段

| 手段 | 打哪堵墙 | 代价 |
|---|---|---|
| 相对位置编码（RoPE/ALiBi） | 位置墙：让模型泛化到更长输入 | 需配合微调才彻底 |
| 长上下文训练（位置插值/渐进拉长） | 位置墙：把没见过的长度学进来 | 训练成本 |
| KV cache 量化 / 滑动窗口 | 显存墙：缓存瘦身或只留最近 N 个 | 滑动窗口会"忘"中间 |
| FlashAttention（分块计算） | O(n²) 的显存墙：省显存、变快 | 不省算力 |
| 稀疏/线性注意力 | O(n²) 的算力墙：只让部分 token 互动 | 质量有代价 |

### 5. 训练和推理的上下文不一样长

- **训练**：长文本切成固定块（block_size），块内做 next-token 预测。模型见过的
  上下文**最长 = block_size**
- **推理**：序列不断变长——每个新生成的 token 都成为下一个 token 的上下文

所以"训练上下文 512"≠"只能处理 512 的输入"。正弦/RoPE 位置编码允许一定外推，
但那是"没学过的地形"，质量会衰减。真实世界"训练 4K 支持 128K"的宣传背后，
是位置插值 + 长上下文微调这些真实技术在撑。

### 6. 回到这个项目

项目里每个和 block_size / max_len 有关的数字，都是上下文的体现：

- stage1~6 的 block 从 64 → 128，stage7 到 256（可选 512）——每一步都在
  "让模型回头看得更远"
- 生成时 `ids[-max_len:]`（取最近 max_len 个）——**上下文窗口上限的代码形态**：
  超出的最早 token 被丢弃，模型"忘了"开头
- 阶段四的 BPE 实验说过"同样 64 的窗口，BPE 看到 128 个字符"——分词是
  压缩上下文之外，提高"信息密度"的第二条路
- stage5 的结论"风格能学到极致、连贯跨不过去"，部分原因也是 128 的上下文
  装不下跨段逻辑——stage7 把它加到 256/512，是连贯改善的变量之一

### 7. 一句话总结

> **上下文 = 模型每次预测时能回头参考的那串 token，是它唯一的"工作记忆"：
> 对话、长文、RAG、few-shot 全发生在上下文里；而"参数里的知识（训练注入、
> 长期、共享）"与"上下文里的信息（输入提供、短期、临场）"的分工，是理解
> LLM 行为的钥匙。窗口有三堵墙——注意力的平方增长、位置编码的外推极限、
> KV cache 的显存吞噬；所有长上下文技术，都是在拆这三堵墙。**
